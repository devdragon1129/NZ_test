"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var loadAd_1 = __importStar(require("./Ad/loadAd"));
var createAd_1 = __importDefault(require("./Ad/createAd"));
var createContent_1 = require("./Ad/createContent");
// pre-fetch add configuration
// loadAllAds()
var ad = {
    loadAd: loadAd_1.default,
    loadAllAds: loadAd_1.loadAllAds,
    createAd: createAd_1.default,
    load: function load() {
        var root = document.querySelector('html');
        var elements = Array.from(document.querySelectorAll('[data-ad-slot]')).filter(function (element) { return Boolean(element.dataset.adSlot); });
        var index = new Map(elements.map(function (element) { return [
            element.dataset.adSlot || '',
            element,
        ]; }));
        Promise.all(elements.map(function (element) {
            return loadAd_1.default(element.dataset.adSlot);
        })).then(function (ads) {
            ;
            (ads || []).filter(Boolean).forEach(function (options) {
                var element = index.get(options.slotName);
                var lang = createContent_1.Locale[element.dataset.adLang] ||
                    createContent_1.Locale[root.lang] ||
                    createContent_1.Locale.en;
                var ad = createAd_1.default(__assign(__assign({}, options), { type: element.dataset.adType || 'regular' }), lang);
                while (element.firstChild) {
                    element.removeChild(element.firstChild);
                }
                element.appendChild(ad);
            });
        });
    },
};
Object.assign(window, { ad: ad });
//# sourceMappingURL=ad.js.map