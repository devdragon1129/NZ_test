import { ContentType, ContentProps } from './types';
export interface ImageProps {
    src: string;
    width: string | number;
    height: string | number;
    top: string | number;
    left: string | number;
    bottom: string | number;
    right: string | number;
}
export default function createImageContent(type: ContentType.Image, props: ContentProps<Partial<ImageProps>>): null | HTMLDivElement;
