"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var types_1 = require("./types");
var utils_1 = require("../utils");
var utils_2 = require("./utils");
function createImageContent(type, props) {
    if (type !== types_1.ContentType.Image) {
        return null;
    }
    var image = document.createElement('div');
    image.className = 'DCL__Ad__Image';
    var width = utils_1.measure(props.width, 100);
    var height = utils_1.measure(props.width, 100);
    image.style.backgroundImage = "url(\"" + props.src + "\")";
    image.style.width = width;
    image.style.height = height;
    if (!utils_2.isFalsy(props.bottom)) {
        image.style.bottom = utils_1.measure(props.bottom);
    }
    else if (!utils_2.isFalsy(props.top)) {
        image.style.top = utils_1.measure(props.top);
    }
    if (!utils_2.isFalsy(props.right)) {
        image.style.right = utils_1.measure(props.right);
    }
    else if (!utils_2.isFalsy(props.left)) {
        image.style.left = utils_1.measure(props.left);
    }
    return image;
}
exports.default = createImageContent;
//# sourceMappingURL=createImageContent.js.map