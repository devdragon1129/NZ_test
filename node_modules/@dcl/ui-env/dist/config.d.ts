import { Env } from './env';
export interface IConfig {
    get: (name: string, defaultValue?: string) => string;
    is(env: Env): boolean;
    getEnv(): Env;
}
export declare type ConfigByEnv = Partial<Record<Env, Record<string, string>>>;
/**
 * Creates a config for a UI
 * @param configByEnv - A record of variables for each Env
 * @returns Config
 */
export declare function createConfig(configByEnv: ConfigByEnv): IConfig;
//# sourceMappingURL=config.d.ts.map