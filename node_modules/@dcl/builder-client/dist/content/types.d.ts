/// <reference types="node" />
export declare type Content = Uint8Array | Blob | ArrayBuffer | Buffer;
export declare type SortedContent<T extends Content> = {
    male: Record<string, T>;
    female: Record<string, T>;
    all: Record<string, T>;
};
export declare type HashedContent = Record<string, string>;
export declare type RawContent<T extends Content> = Record<string, T>;
