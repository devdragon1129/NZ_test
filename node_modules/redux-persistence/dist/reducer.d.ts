import { PayloadAction } from 'typesafe-actions';
import { StateMerger } from './types';
export default function <T, A>(reducer: any, merger?: StateMerger): (state: T, action: PayloadAction<any, any>) => any;
