import { Action } from 'typesafe-actions';
import { StorageEngine, MiddlewareOptions, ReduxStore } from './types';
export declare function createMiddleware<T extends any>(engine: StorageEngine<T>, options?: MiddlewareOptions<T>): (store: ReduxStore<T>) => (next: any) => (action: Action<string>) => any;
